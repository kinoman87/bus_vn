<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gena.bus_vn" >
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:textColor="#ffffff"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bus_schedule"></activity>
        <activity android:name=".traffic_patterns"></activity>
        <activity android:name=".bus_list"></activity>
        <activity android:name=".information"></activity>
        <activity android:name=".time"></activity>
        <activity android:name=".timetable"></activity>

    </application>
</manifest>
